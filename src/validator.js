const validator = {
    isValid: function (creditCardNumber) {

    //convertir un string en un array
        const digits= Array.from(creditCardNumber) 
    //recorrer cada digito
    //en los digitos pares iniciando de 0
    //se multiplican por dos 
    //si el resultado es igual o mayores de 10 sumar los dos digitos
      
        
    }


};


export default validator;
